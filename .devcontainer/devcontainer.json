{
    "name": "Hangrier Games",
    "dockerComposeFile": "docker-compose.yml",
    "service": "dev",
    "workspaceFolder": "/workspaces/hangrier_games",
    "shutdownAction": "stopCompose",

    "features": {
        "ghcr.io/devcontainers/features/github-cli:1": {},
        "ghcr.io/guiyomh/features/just:0": {},
        "ghcr.io/eitsupi/devcontainer-features/jujutsu-cli:1": {},
        "//": "node:1 — provides Node.js for dev tooling (Ollama, SurrealDB CLI wrappers, formatters). Pinned to v1 track for stability.",
        "ghcr.io/devcontainers/features/node:1": {}
    },

    "forwardPorts": [3000, 8000, 8025],
    "portsAttributes": {
        "3000": { "label": "API", "onAutoForward": "notify" },
        "8000": { "label": "SurrealDB", "onAutoForward": "silent" },
        "8025": { "label": "Mailpit", "onAutoForward": "notify" }
    },

    "remoteEnv": {
        "ENV": "development",
        "SURREAL_HOST": "ws://localhost:8000",
        "SURREAL_USER": "root",
        "SURREAL_PASS": "root",
        "EMAIL_BACKEND": "mailpit",
        "EMAIL_FROM": "noreply@hangrier.fun",
        "MAILPIT_HOST": "localhost",
        "MAILPIT_PORT": "1025"
    },

    "postCreateCommand": "bash .devcontainer/post-create.sh",

    "customizations": {
        "vscode": {
            "extensions": [
                "rust-lang.rust-analyzer",
                "tamasfe.even-better-toml",
                "vadimcn.vscode-lldb",
                "skellock.just",
                "GitHub.vscode-pull-request-github"
            ],
            "settings": {
                "rust-analyzer.cargo.targetDir": true,
                "editor.formatOnSave": true,
                "[rust]": {
                    "editor.defaultFormatter": "rust-lang.rust-analyzer"
                }
            }
        }
    },

    "remoteUser": "vscode"
}
